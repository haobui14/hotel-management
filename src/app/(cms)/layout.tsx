export const metadata = {
  title: "Luxury Hotel Management - Studio",
  description: "Content management system for Luxury Hotel Management",
};

export default function CMSLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
